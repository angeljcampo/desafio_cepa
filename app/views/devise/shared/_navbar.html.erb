<nav class="navbar navbar-expand-lg navbar-dark bg-dark">
  <%= link_to "Winestrain", root_path, class: "h2 text-danger" %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav ml-auto mr-5">
    <%  if signed_in?  %>
        <% if current_user.admin?  %>
          <div class="btn btn-light mr-3">
            <%= link_to "Create Wine", new_wine_path  %>
          </div>
          <div class="btn btn-light mr-3">
            <%= link_to "Create Strain", new_strain_path  %>
          </div>
        <% end %>
        <div class="btn btn-danger">
          <%= link_to "Salir", destroy_user_session_path, method: :delete, class: "text-light" %>
        </div>

      <% else %>
        <li class="nav-item mr-3">
          <%= link_to "Registrarse", new_user_registration_path,  class:"text-light" %>
        </li>
        <li class="nav-item">
          <%= link_to "Ingresar", new_user_session_path,  class: "text-light" %>
        </li>
  <% end %>
    </ul>
  </div>
</nav>